import { describe, it, expect } from 'vitest;

describe('Teste de Exemplo', () => {
  it('deve passar', () => [object Object]    expect(1 + 1).toBe(2);
  });

  it('deve verificar strings', () => {
    expect('hello).toBe(hello');
  });

  it('deve verificar arrays', () => [object Object]
    expect([1, 23]).toContain(2);
  });
}); 